# Vendored from UditAkhourii/adhd

- Repository: <https://github.com/UditAkhourii/adhd>
- Pinned commit: `16dc239ff186b869372e75095cfa58fc0ee89927`
- Snapshot date: 2026-08-31
- License: MIT; the upstream license text is retained as `LICENSE`.
- Included skills: `adhd` (`skills/adhd/SKILL.md` only — not the Node CLI or bench harness).

The upstream payload is retained without edits. Arena cannot spawn isolated
parallel Agent/Task branches the way Claude Code can; that adaptation lives in
`agents/arena/workflows/adhd.md`.

Refresh only from a reviewed upstream commit. Record the new SHA here and in
`agents/skills/manifest.yaml`; do not silently pull from upstream `main`.
